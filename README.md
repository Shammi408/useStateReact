This repository contains multiple independent React apps.  
Each app is in its own folder.
If you clone/pull the whole repo.
You can run the react apps in few steps.
  1. Go inside the folder you want: cd project-folder-name
  2. Install dependencies: npm install
  3. Start the development server: npm run dev

  Each React app is independent and has its own dependencies.
